# Simpan sebagai .env.local di ROOT frontend (bukan di dalam src)
# Saat backend jalan lokal:
VITE_API_BASE=http://localhost:4000/api/v1

# Saat backend sudah di Render, ganti jadi:
# VITE_API_BASE=https://nama-service-kamu.onrender.com/api/v1
